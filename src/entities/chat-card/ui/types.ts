export interface IChatCard {
  id: string;
  title: string;
  text: string;
  avatar: string;
  dateString: string;
}

export interface ITextContent {
  title: string;
  text: string;
}

export interface ITime {
  dateString: string;
}
