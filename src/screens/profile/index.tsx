import { Text } from 'react-native';
import { MainLayout } from 'shared/layout/main';

export const ProfileScreen = () => {
  return (
    <MainLayout>
      <Text>Profile</Text>
    </MainLayout>
  );
};
